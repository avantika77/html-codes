#text-input {
    width: 300px;
    height: 30px;
    transition: border-color 0.3s, transform 0.3s;
    border: 2px solid #ccc;
}

#text-input:focus {
    border-color: #007BFF;
    transform: scale(1.05);
}
